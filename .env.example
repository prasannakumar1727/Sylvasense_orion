# Google Earth Engine service-account credentials (free academic tier)
GEE_SERVICE_ACCOUNT=
GEE_PRIVATE_KEY_PATH=
# Postgres (optional — SQLite is the default for the MVP)
DATABASE_URL=sqlite:///./sylvasense.db
